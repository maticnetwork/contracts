dist: xenial
sudo: required
group: beta
services:
  - docker
# Replace with your image name.
before_script:
  - docker pull parity/parity
  - docker pull node:8
matrix:
  fast_finish: true
before_script:
  - export PATH=/snap/bin:${PATH}
  - npm install
script:
  - docker run -it parity/parity
  - docker run -it node:8
  - npm run test:ci


